# Pinned to exact versions with pre-built wheels for Python 3.11
# Avoids slow/broken source compilation on Render free tier

# Trading engine
numpy==1.26.4
pandas==2.1.4
yfinance==0.2.40
pytz==2024.1

# API server
fastapi==0.110.0
uvicorn[standard]==0.29.0
websockets==12.0
